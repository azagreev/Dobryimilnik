---
phase: 02-livemaster-import-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - backend/app/domains/imports/repository.py
  - backend/app/domains/imports/service.py
  - backend/tests/test_imports_idempotency.py
autonomous: true
requirements:
  - IMP-04
  - IMP-05
must_haves:
  truths:
    - "IMP-04: re-running the same CSV does not duplicate source mappings or staging identities."
    - "D-09: stable source IDs plus content hashes drive idempotency; hash-only matching is not the primary strategy."
    - "D-10: rows are classified as new, unchanged, changed, skipped, failed, or requires_review."
    - "This plan replaces the roadmap's older canonical publish wording with staging-only rerun classification per 02-CONTEXT.md."
  artifacts:
    - "backend/app/domains/imports/service.py"
    - "backend/app/domains/imports/repository.py"
    - "backend/tests/test_imports_idempotency.py"
  key_links:
    - "Service computes content hashes from normalized row payloads."
    - "Repository upserts source mappings using the uniqueness from 02-02."
---

<objective>
Implement idempotent staging reruns and row classification without publishing to canonical tables.
</objective>

<execution_context>
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md
@.planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md
@.planning/phases/02-livemaster-import-pipeline/02-02-SUMMARY.md
@.planning/phases/02-livemaster-import-pipeline/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic content hashing and source mapping classification</name>
  <files>backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, backend/tests/test_imports_idempotency.py</files>
  <read_first>backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md</read_first>
  <action>Implement deterministic content hashing for normalized row payloads. Use source system, entity type, source ID, and content hash to classify rows against existing source mappings: `new`, `unchanged`, or `changed`. Preserve `failed` from validation and allow `skipped`/`requires_review` where service rules need them. Keep classification in the imports service and persistence in the repository.</action>
  <verify>cd backend && uv run pytest tests/test_imports_idempotency.py</verify>
  <acceptance_criteria>
    - Same source ID and same content hash is classified `unchanged` on rerun.
    - Same source ID and different content hash is classified `changed`.
    - New source ID is classified `new`.
  </acceptance_criteria>
  <done>Rows are classified deterministically for idempotent staging reruns.</done>
</task>

<task type="auto">
  <name>Task 2: Prove reruns do not duplicate staging identities</name>
  <files>backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, backend/tests/test_imports_idempotency.py</files>
  <read_first>backend/app/domains/imports/models.py, backend/app/domains/imports/repository.py, backend/app/domains/imports/service.py</read_first>
  <action>Add tests and service safeguards proving that processing the same CSV content twice does not duplicate source mappings for the same source entity. If database-backed PostgreSQL tests are available, exercise the real unique constraint/upsert path. If not, test the service/repository contract and document remaining DB-backed verification in the summary. Do not create canonical records.</action>
  <verify>cd backend && uv run pytest tests/test_imports_idempotency.py tests/test_imports_repository.py</verify>
  <acceptance_criteria>
    - Rerun tests cover duplicate source IDs across batches.
    - Duplicate source mappings are not created.
    - No code writes to catalog/content/users/orders canonical tables.
  </acceptance_criteria>
  <done>Idempotent rerun behavior is covered by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation service -> idempotency classifier | Validated row data becomes durable source identity state. |
| Idempotency classifier -> source mappings | Upsert/classification mutates cross-batch identity state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | Content hash | mitigate | Hash normalized payload deterministically and never use hash alone as source identity. |
| T-02-11 | Repudiation | Rerun classification | mitigate | Persist row classification and batch/source mapping IDs. |
| T-02-12 | Elevation of Privilege | Canonical write bypass | mitigate | Keep Phase 2 staging-only and test absence of canonical writes. |
</threat_model>

<verification>
- [ ] `cd backend && uv run pytest tests/test_imports_idempotency.py tests/test_imports_repository.py` exits 0.
- [ ] Same CSV rerun does not duplicate source mappings.
- [ ] Changed content is classified as changed.
- [ ] No canonical publish code exists.
</verification>

<success_criteria>
- Source ID plus content hash idempotency is implemented.
- Reruns are safe and test-covered.
- Phase 2 remains staging-only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-livemaster-import-pipeline/02-04-SUMMARY.md`.
</output>
