---
phase: 02-livemaster-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/router.py
  - backend/app/domains/imports/__init__.py
  - backend/app/domains/imports/api.py
  - backend/app/domains/imports/csv_contract.py
  - backend/app/domains/imports/models.py
  - backend/app/domains/imports/repository.py
  - backend/app/domains/imports/schemas.py
  - backend/app/domains/imports/service.py
  - backend/tests/test_domain_packages.py
  - backend/tests/test_imports_contract.py
autonomous: true
requirements:
  - IMP-01
  - IMP-03
must_haves:
  truths:
    - "IMP-01: backend accepts a single CSV file as the Phase 2 import contract."
    - "D-01 and D-02: CSV contract is explicit and validates required headers before row processing."
    - "D-04: this plan creates import batch scaffolding only; it does not publish to canonical domain tables."
    - "D-14 and D-15: API schemas anticipate batch status and error-table fields."
    - "The imports domain follows the Phase 1 `API -> service -> repository` pattern."
  artifacts:
    - "backend/app/domains/imports/api.py"
    - "backend/app/domains/imports/csv_contract.py"
    - "backend/app/domains/imports/schemas.py"
    - "backend/tests/test_imports_contract.py"
  key_links:
    - "backend/app/api/v1/router.py includes the imports router."
    - "CSV contract tests exercise missing required headers and accepted headers."
---

<objective>
Create the imports domain package, API route skeleton, and versioned single-CSV contract for Livemaster staging imports.
</objective>

<execution_context>
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md
@.planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md
@.planning/phases/02-livemaster-import-pipeline/02-PATTERNS.md
@.planning/phases/02-livemaster-import-pipeline/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add imports domain package and router integration</name>
  <files>backend/app/domains/imports/__init__.py, backend/app/domains/imports/api.py, backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, backend/app/domains/imports/models.py, backend/app/domains/imports/schemas.py, backend/app/api/v1/router.py, backend/tests/test_domain_packages.py</files>
  <read_first>backend/app/api/v1/router.py, backend/app/domains/catalog/api.py, backend/tests/test_domain_packages.py, .planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md, .planning/phases/02-livemaster-import-pipeline/02-PATTERNS.md</read_first>
  <action>Create `backend/app/domains/imports/` with importable `api`, `service`, `repository`, `models`, and `schemas` modules following the existing domain skeleton. Add an `APIRouter` in `imports/api.py` and include it from `backend/app/api/v1/router.py` under a stable import route prefix such as `/imports`. Update domain importability tests so `imports` is covered alongside existing domains. Keep routes minimal in this plan; do not implement canonical publication or admin UI.</action>
  <verify>cd backend && uv run pytest tests/test_domain_packages.py</verify>
  <acceptance_criteria>
    - `backend/app/domains/imports` contains `api.py`, `service.py`, `repository.py`, `models.py`, and `schemas.py`.
    - `backend/app/api/v1/router.py` includes the imports router.
    - `backend/tests/test_domain_packages.py` imports `app.domains.imports.*`.
  </acceptance_criteria>
  <done>Imports domain exists, is routed, and is importable.</done>
</task>

<task type="auto">
  <name>Task 2: Define single CSV contract and batch request/response schemas</name>
  <files>backend/app/domains/imports/csv_contract.py, backend/app/domains/imports/schemas.py, backend/tests/test_imports_contract.py</files>
  <read_first>.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md, backend/app/domains/imports/schemas.py</read_first>
  <action>Create `csv_contract.py` with a versioned contract for one CSV file. Require at minimum `entity_type` and `source_id`; preserve `row_number`; define supported entity types matching the research (`product`, `variant`, `category`, `media`, `post`, `review`, `customer`, `order`). Add Pydantic schemas for batch creation/status and error-table rows with row number, entity type, field, code, message, and severity. Add tests for accepted headers, missing required headers, row-number preservation, and unsupported entity type detection.</action>
  <verify>cd backend && uv run pytest tests/test_imports_contract.py</verify>
  <acceptance_criteria>
    - CSV contract requires `entity_type` and `source_id`.
    - Tests prove missing required headers are rejected before row processing.
    - Schemas expose batch status and import error table row fields.
  </acceptance_criteria>
  <done>CSV contract and API schemas are explicit enough for later persistence and validation tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner/client -> CSV upload API | Untrusted CSV enters backend parsing code. |
| API layer -> imports service | Route inputs become structured import commands. |
| Imports service -> future persistence | Contract decisions constrain later staging writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CSV upload | mitigate | Require explicit headers and supported entity types before processing rows. |
| T-02-02 | Information Disclosure | Validation responses | mitigate | Return structured errors without dumping full raw rows in API responses. |
| T-02-03 | Denial of Service | CSV file size/shape | mitigate | Plan contract-level rejection for invalid structure and leave file-size guard hooks for implementation. |
</threat_model>

<verification>
- [ ] `cd backend && uv run pytest tests/test_domain_packages.py tests/test_imports_contract.py` exits 0.
- [ ] Imports router is included under `/api/v1`.
- [ ] CSV contract rejects missing `entity_type` or `source_id`.
- [ ] No canonical catalog/content/users/orders models are created.
</verification>

<success_criteria>
- A single-CSV import contract exists and is tested.
- Imports domain follows established backend layering.
- API schemas support batch status and future error-table responses.
</success_criteria>

<output>
After completion, create `.planning/phases/02-livemaster-import-pipeline/02-01-SUMMARY.md`.
</output>
