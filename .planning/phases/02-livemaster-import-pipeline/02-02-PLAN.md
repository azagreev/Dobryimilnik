---
phase: 02-livemaster-import-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/alembic/versions/0002_create_import_staging.py
  - backend/app/domains/imports/models.py
  - backend/app/domains/imports/repository.py
  - backend/tests/test_imports_repository.py
  - backend/tests/test_migrations.py
autonomous: true
requirements:
  - IMP-01
  - IMP-04
must_haves:
  truths:
    - "IMP-01: staging tables preserve source IDs and batch metadata."
    - "IMP-04: database constraints support idempotent reruns without duplicate source mappings."
    - "D-04: only staging schema/tables are created; no canonical publication tables are introduced."
    - "D-07 and D-08: shared `source_mappings` is the source identity mechanism."
  artifacts:
    - "backend/alembic/versions/0002_create_import_staging.py"
    - "backend/app/domains/imports/models.py"
    - "backend/app/domains/imports/repository.py"
    - "backend/tests/test_imports_repository.py"
  key_links:
    - "Migration creates `staging.import_batches`, `staging.import_rows`, `staging.source_mappings`, and `staging.import_errors`."
    - "`source_mappings` has a uniqueness guarantee for source system, entity type, and source ID."
---

<objective>
Create the staging schema, import persistence models, and repository foundation for source identity preservation.
</objective>

<execution_context>
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md
@.planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md
@.planning/phases/02-livemaster-import-pipeline/02-PATTERNS.md
@.planning/phases/02-livemaster-import-pipeline/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staging schema migration and SQLAlchemy models</name>
  <files>backend/alembic/versions/0002_create_import_staging.py, backend/app/domains/imports/models.py, backend/tests/test_migrations.py</files>
  <read_first>backend/alembic/env.py, backend/alembic/versions/0001_create_domain_schemas.py, backend/app/core/db.py, .planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md</read_first>
  <action>Create Alembic revision `0002_create_import_staging.py` that creates schema `staging` and tables for `import_batches`, `import_rows`, `source_mappings`, and `import_errors`. Add SQLAlchemy models using `schema = "staging"`. Include status/severity/entity type columns, JSON payload fields where appropriate, timestamps, indexes for batch/error lookup, and uniqueness on `(source_system, entity_type, source_id)` in `source_mappings`. Downgrade must drop staging tables and schema safely. Update migration tests to assert Phase 2 introduces `staging` and source mapping uniqueness while Phase 1 stays unchanged.</action>
  <verify>cd backend && uv run pytest tests/test_migrations.py</verify>
  <acceptance_criteria>
    - Migration contains `CREATE SCHEMA IF NOT EXISTS staging` or Alembic equivalent.
    - Migration creates all four required staging tables.
    - `source_mappings` has a unique constraint or index covering source system, entity type, and source ID.
    - Downgrade removes Phase 2 staging objects without dropping existing domain schemas.
  </acceptance_criteria>
  <done>Staging schema and models exist with source identity constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository methods for batches, rows, source mappings, and errors</name>
  <files>backend/app/domains/imports/repository.py, backend/tests/test_imports_repository.py</files>
  <read_first>backend/app/core/db.py, backend/app/domains/imports/models.py, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md</read_first>
  <action>Implement repository methods to create an import batch, add import rows, add structured errors, fetch batch summaries, and upsert/find source mappings. Use SQLAlchemy async session patterns. If PostgreSQL upsert behavior cannot be fully executed in local unit tests yet, isolate SQL construction enough to test it and document any PostgreSQL-backed verification needed in the summary. Do not implement parser/validation policy here beyond data access primitives.</action>
  <verify>cd backend && uv run pytest tests/test_imports_repository.py</verify>
  <acceptance_criteria>
    - Repository exposes batch, row, source mapping, and error persistence methods.
    - Tests cover at least method contracts and source mapping uniqueness/upsert intent.
    - Repository does not publish to canonical domain tables.
  </acceptance_criteria>
  <done>Persistence layer supports later validation and idempotency logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration process -> PostgreSQL | Schema changes create durable staging structures. |
| Imports repository -> database | Import data and source identities are persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Source mappings | mitigate | Enforce database uniqueness for source identity. |
| T-02-05 | Repudiation | Import batches | mitigate | Persist batch IDs, row statuses, errors, timestamps, and source IDs. |
| T-02-06 | Information Disclosure | Raw staging payloads | mitigate | Store raw payloads in staging but avoid exposing them directly in owner-facing API schemas. |
</threat_model>

<verification>
- [ ] `cd backend && uv run pytest tests/test_migrations.py tests/test_imports_repository.py` exits 0.
- [ ] `staging` schema and required tables are present in migration.
- [ ] Source mapping uniqueness is represented in DB DDL/model/tests.
- [ ] No canonical publish code exists.
</verification>

<success_criteria>
- Import batches, rows, source mappings, and errors are persistable.
- Source identity has a durable uniqueness guarantee.
- Staging schema is reversible through Alembic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-livemaster-import-pipeline/02-02-SUMMARY.md`.
</output>
