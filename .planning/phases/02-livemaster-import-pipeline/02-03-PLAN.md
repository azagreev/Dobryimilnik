---
phase: 02-livemaster-import-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - backend/app/domains/imports/csv_contract.py
  - backend/app/domains/imports/service.py
  - backend/app/domains/imports/validation.py
  - backend/tests/test_imports_validation.py
autonomous: true
requirements:
  - IMP-02
  - IMP-05
must_haves:
  truths:
    - "IMP-02: imported rows are validated before any later publication path can use them."
    - "IMP-05: products, variants, categories, media, posts, reviews, customers, and orders are recognized as source entity types when available."
    - "D-11: critical row errors fail only the affected row while batch processing continues."
    - "D-13: row validation emits structured field/code/message/severity errors."
  artifacts:
    - "backend/app/domains/imports/validation.py"
    - "backend/app/domains/imports/service.py"
    - "backend/tests/test_imports_validation.py"
  key_links:
    - "CSV parser produces row-numbered records."
    - "Validation service persists row errors through the repository from 02-02."
---

<objective>
Implement row parsing, normalization, and strict per-row validation for the single-CSV import pipeline.
</objective>

<execution_context>
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md
@.planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md
@.planning/phases/02-livemaster-import-pipeline/02-VALIDATION.md
@.planning/phases/02-livemaster-import-pipeline/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build row parser and normalized internal row representation</name>
  <files>backend/app/domains/imports/csv_contract.py, backend/app/domains/imports/service.py, backend/tests/test_imports_validation.py</files>
  <read_first>backend/app/domains/imports/csv_contract.py, backend/app/domains/imports/schemas.py, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md</read_first>
  <action>Extend CSV handling so uploaded CSV content becomes row-numbered internal records. Preserve raw row payload and produce normalized fields needed by validation: entity type, source ID, optional parent source ID, and content hash input. Keep parsing deterministic and easy to unit test with in-memory CSV strings. Missing required headers should remain a batch-level error from 02-01; malformed individual rows should become row-level errors.</action>
  <verify>cd backend && uv run pytest tests/test_imports_validation.py</verify>
  <acceptance_criteria>
    - Parser preserves original CSV row numbers.
    - Parser recognizes supported entity types.
    - Parser keeps raw row payload available for staging/audit.
  </acceptance_criteria>
  <done>CSV rows can be parsed into stable internal records.</done>
</task>

<task type="auto">
  <name>Task 2: Implement entity-specific strict row validation</name>
  <files>backend/app/domains/imports/validation.py, backend/app/domains/imports/service.py, backend/tests/test_imports_validation.py</files>
  <read_first>.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md, backend/app/domains/imports/repository.py</read_first>
  <action>Create validation rules for each supported entity type. Enforce required identity fields for all rows and entity-specific required fields where the single CSV contract defines them. Implement structured errors with `field`, `code`, `message`, `severity`, `row_number`, and `entity_type`. Wire service logic so a failed row is recorded as failed and later rows continue. Avoid canonical writes.</action>
  <verify>cd backend && uv run pytest tests/test_imports_validation.py</verify>
  <acceptance_criteria>
    - Invalid row creates structured errors and does not stop the batch.
    - Valid rows can be passed to staging persistence.
    - Tests cover at least one valid row and one failed row for key entity types.
  </acceptance_criteria>
  <done>Row validation is strict, structured, and batch-continuing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV parser -> validation service | Text input becomes typed/normalized row data. |
| Validation service -> staging repository | Only validated/classified rows should be persisted as non-failed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Row validation | mitigate | Use explicit per-entity required fields and error codes. |
| T-02-08 | Denial of Service | Bad row sequences | mitigate | Continue processing after row failures but avoid unbounded exception propagation. |
| T-02-09 | Information Disclosure | Error messages | mitigate | Return owner-readable messages without leaking stack traces. |
</threat_model>

<verification>
- [ ] `cd backend && uv run pytest tests/test_imports_validation.py` exits 0.
- [ ] Failed rows produce structured errors.
- [ ] Batch processing continues after a failed row.
- [ ] No canonical write path is introduced.
</verification>

<success_criteria>
- CSV rows are normalized and validated.
- Validation covers supported import entity types.
- Row errors are structured for the later owner-facing error table.
</success_criteria>

<output>
After completion, create `.planning/phases/02-livemaster-import-pipeline/02-03-SUMMARY.md`.
</output>
