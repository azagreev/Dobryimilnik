---
phase: 02-livemaster-import-pipeline
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - backend/app/domains/imports/api.py
  - backend/app/domains/imports/csv_contract.py
  - backend/app/domains/imports/repository.py
  - backend/app/domains/imports/schemas.py
  - backend/app/domains/imports/service.py
  - backend/tests/test_imports_api.py
autonomous: true
requirements:
  - IMP-03
  - IMP-02
  - IMP-04
must_haves:
  truths:
    - "IMP-01: backend exposes an endpoint an admin/client can call to run a CSV import into staging."
    - "IMP-03: backend exposes an import audit report with imported, skipped, failed, and manual-review counts."
    - "D-14 and D-15: backend exposes a batch error table with row number, entity type, field, code, message, and severity."
    - "D-16: detailed error-table API is required; download-only CSV report is not enough."
    - "The API remains backend-only; full admin UI is deferred."
  artifacts:
    - "backend/app/domains/imports/api.py"
    - "backend/app/domains/imports/schemas.py"
    - "backend/tests/test_imports_api.py"
  key_links:
    - "API routes read data persisted by prior import plans."
    - "API tests verify response shape for batch summary and error rows."
---

<objective>
Expose backend API endpoints for import batch status, audit summary, and owner-facing error-table review.
</objective>

<execution_context>
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/Lenovo/Documents/GitHub/Dobryimilnik/Dobryimilnik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md
@.planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md
@.planning/phases/02-livemaster-import-pipeline/02-VALIDATION.md
@.planning/phases/02-livemaster-import-pipeline/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement batch status and audit summary API</name>
  <files>backend/app/domains/imports/api.py, backend/app/domains/imports/csv_contract.py, backend/app/domains/imports/schemas.py, backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, backend/tests/test_imports_api.py</files>
  <read_first>backend/app/domains/imports/api.py, backend/app/domains/imports/csv_contract.py, backend/app/domains/imports/schemas.py, backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, .planning/phases/02-livemaster-import-pipeline/02-CONTEXT.md</read_first>
  <action>Implement API route(s) to run a single-CSV import into staging and fetch import batch status/summary counts. The run endpoint should accept FastAPI `UploadFile` or an equivalent testable upload abstraction, call the imports service, and return the created batch ID/status. Summary must include enough counts to satisfy IMP-03: imported/staged, skipped, failed, and manually required/requires_review records. Use service/repository methods rather than direct DB access from API. Keep route naming stable for future admin UI.</action>
  <verify>cd backend && uv run pytest tests/test_imports_api.py</verify>
  <acceptance_criteria>
    - Import run endpoint accepts one CSV upload and returns batch ID/status.
    - Batch status endpoint returns batch ID, status, and summary counts.
    - Summary includes failed and requires-review/manual-review counts.
    - API tests exercise the route through the FastAPI app.
  </acceptance_criteria>
  <done>Backend exposes import batch audit summary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement paginated error-table API</name>
  <files>backend/app/domains/imports/api.py, backend/app/domains/imports/schemas.py, backend/app/domains/imports/service.py, backend/app/domains/imports/repository.py, backend/tests/test_imports_api.py</files>
  <read_first>backend/app/core/pagination.py, backend/app/domains/imports/schemas.py, backend/app/domains/imports/repository.py, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md</read_first>
  <action>Implement error-table route for a batch with pagination parameters consistent with existing core pagination helpers where applicable. Response rows must include CSV row number, entity type, field, code, message, and severity. Avoid returning raw row payloads by default. Add tests for response shape, pagination behavior, and stable error fields.</action>
  <verify>cd backend && uv run pytest tests/test_imports_api.py</verify>
  <acceptance_criteria>
    - Error table endpoint returns row number, entity type, field, code, message, and severity.
    - Raw row payload is not returned by default.
    - Pagination is represented in request/response or tested as a stable list contract.
  </acceptance_criteria>
  <done>Owner-facing backend error table is available for future admin UI.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase-level verification and document residual gaps</name>
  <files>backend/tests/test_imports_api.py, .planning/phases/02-livemaster-import-pipeline/02-05-SUMMARY.md</files>
  <read_first>.planning/phases/02-livemaster-import-pipeline/02-VALIDATION.md, .planning/phases/02-livemaster-import-pipeline/02-RESEARCH.md</read_first>
  <action>Run the focused import tests and the full backend test suite. In the summary, record exact commands and any residual gaps such as missing real PostgreSQL upsert verification if the test suite remains source/unit-only. Do not mark manual owner-message review as completed unless actually performed.</action>
  <verify>cd backend && uv run pytest tests/test_imports_contract.py tests/test_imports_validation.py tests/test_imports_repository.py tests/test_imports_idempotency.py tests/test_imports_api.py && uv run pytest</verify>
  <acceptance_criteria>
    - Focused import tests pass.
    - Full backend tests pass or failures are documented as blockers.
    - Summary records residual manual-only verification for owner-readable messages.
  </acceptance_criteria>
  <done>Phase 2 backend import API is verified and ready for `$gsd-verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API client -> import report endpoints | Owner/admin-facing clients read import status and errors. |
| Report service -> staging database | Report code reads persisted batch, row, and error data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | Error table API | mitigate | Do not expose raw row payloads by default; return structured fields only. |
| T-02-14 | Repudiation | Import audit summary | mitigate | Return durable batch ID, status, counts, and persisted errors. |
| T-02-15 | Denial of Service | Error listing | mitigate | Add pagination or bounded list behavior for batch errors. |
</threat_model>

<verification>
- [ ] `cd backend && uv run pytest tests/test_imports_api.py` exits 0.
- [ ] `cd backend && uv run pytest` exits 0.
- [ ] Import run endpoint accepts one CSV upload and creates a staging batch.
- [ ] Batch summary includes imported/staged, skipped, failed, and requires-review counts.
- [ ] Error table exposes row number, entity type, field, code, message, and severity.
- [ ] Raw row payload is not returned by default.
</verification>

<success_criteria>
- Admin/owner can inspect import batch status through backend API.
- Admin/owner can inspect validation errors before records are published.
- Import reports identify success, skipped rows, failures, and manual-review items.
- Phase 2 remains backend-only and staging-only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-livemaster-import-pipeline/02-05-SUMMARY.md`.
</output>
